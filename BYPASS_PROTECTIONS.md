# Bypass protections

The full security/protections document moved to the
`sandbox-core-lib` package, where the implementing modules live:

- **`sandbox_core_lib/SANDBOX_PROTECTIONS.md`** — every protection,
  every closed gap, every residual.

This stub stays at the kato repo root so external links and the
git-history pointer survive the move. Updates to the policy go in
the new location.
